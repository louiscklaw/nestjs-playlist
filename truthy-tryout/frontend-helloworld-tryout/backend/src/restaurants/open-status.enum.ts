/**
 * User status enum
 */
export enum OpenStatusEnum {
  // shop is open
  OPEN = 'OPEN',

  // shop is not open again
  CLOSED = 'CLOSED',

  // shop closed
  PERMANENTLY_CLOSED = 'PERMANENTLY_CLOSED'
}
